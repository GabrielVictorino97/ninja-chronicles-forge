# API local (dotnet run)
# IMPORTANTE: o preview roda em HTTPS (lovableproject.com), então o navegador
# bloqueia chamadas HTTP (mixed content). Use a URL HTTPS do Kestrel.
# Se o cert for self-signed, abra https://localhost:7259/swagger uma vez no
# mesmo navegador e aceite o aviso de segurança — depois disso o fetch funciona.
VITE_API_BASE_URL=https://localhost:7259/api
